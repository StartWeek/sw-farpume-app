{
  "name": "erp-parfum-laravel-inertia",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix sw-parfume-app run dev:full",
    "frontend": "npm --prefix sw-parfume-app run dev",
    "build": "npm --prefix sw-parfume-app run build",
    "migrate:fresh": "npm --prefix sw-parfume-app run migrate:fresh",
    "install:frontend": "npm install --prefix sw-parfume-app"
  }
}
